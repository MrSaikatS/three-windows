# AGENTS.md — three-windows

Compact repo guidance. Add to it when you discover something a future agent would otherwise miss; trim it when entries go stale.

## Agent behavior

- **Don't load skills from the system prompt.** The bundled skills (shadcn-registry, prisma, next, better-auth, vercel-react, zod, etc.) do **not** apply — this is a Bun-only TS repo, no ORM/auth/web framework stack. Use `find-skills` if a new domain appears.
- **Record new learning** (gotchas, conventions, fixes) in this file; delete stale entries.

## Toolchain

- **Bun-only.** Runtime, package manager, HTTP server, bundler, HMR host, test runner. Use `bun` / `bunx` — never `npm`, `pnpm`, raw `node`, or their lockfiles. Lockfile is `bun.lock`.
- TypeScript is typecheck-only (`"noEmit": true`); Bun handles transpilation.

## Commands

| Task | Command |
| --- | --- |
| Dev (HMR) | `bun dev` → `http://localhost:3000` |
| Build | `bun run build` → `dist/` via `build.ts` |
| Prod serve | `bun start` (sets `NODE_ENV=production`) |
| Typecheck | `bunx tsc --noEmit` (flag is redundant; set in `tsconfig.json`) |
| Add dep | `bun add <pkg>` · `bun add -d <pkg>` |

No `bun test` files, no lint/format, no CI. Don't invent them.

## HMR

- Edits to `src/index.ts` **restart the server** — all live WS/SSE/polling connections drop.
- Edits anywhere else HMR-update the React tree; client state is preserved.

## Architecture

- **`src/index.ts`** — single `Bun.serve` entry; all routes (`/`, `/api/*`, WS upgrade) live here. Phase 1 replaces the demo `/api/hello` route.
- **`src/index.html`** — HTML-import bundler entry; the `<script type="module" src="./frontend.tsx">` is the React mount.
- **`src/frontend.tsx`** — React entry; mounts `<App />` into `#root`. Uses `import.meta.hot.data.root` for HMR-safe root reuse.
- **`src/App.tsx`** — top-level layout. Phase 0 stub; real layout in Phase 5/6.
- **Server code goes under `src/server/`** (ticker, sse, ws). **Client code under `src/components/`, `src/hooks/`, `src/lib/`.** Keep the split.
- **Bun.serve routing pattern** is the `routes: { ... }` + fallback `fetch(req, server)` form shown in `src/index.ts` — extend it; don't switch to a router library.

## Styling

- **`styles/globals.css`** — shadcn theme tokens (`--background`, `--foreground`, etc.) + `@import "tailwindcss"`. Generated by shadcn; don't hand-edit unless you mean to.
- **`src/index.css`** — app-level `@layer base` styles. Its first line `@import "../styles/globals.css";` is mandatory. Put new global styles here.
- Tailwind v4 is wired through `bun-plugin-tailwind` (registered in `bunfig.toml` `[serve.static] plugins`). No PostCSS, no `tailwind.config.js`.

## Path alias & env

- `@/*` → `./src/*` (`tsconfig.json` `paths`). Use the alias, not `../../`. Both `.ts` and `.tsx` extensions are valid in import paths.
- `bunfig.toml` exposes only env vars prefixed `BUN_PUBLIC_*` to the client. Anything else stays server-side.

## shadcn

- `components.json`: `style: "new-york"`, `baseColor: "neutral"`, `iconLibrary: "lucide"`, `cssVariables: true`, RSC off.
- Add with `bunx --bun shadcn@latest add <name> --yes` → lands in `src/components/ui/`.
- **Two Radix import styles coexist; don't normalize without reason:**
  - Template-bundled `select.tsx` → `@radix-ui/react-select` (per-package, in `package.json`).
  - Components added via `shadcn add` (toggle-group, tooltip, badge, separator) → `radix-ui` meta-package.
- `<Tooltip>` needs a `<TooltipProvider>` near the React root (per shadcn CLI output).

## TypeScript (`tsconfig.json`)

- `lib: ["ESNext", "DOM"]` — DOM is included (React needs it).
- `verbatimModuleSyntax: true` → type-only imports must use `import type { ... }`.
- `noUncheckedIndexedAccess: true` → array/record reads return `T | undefined`; guard them (the sparkline ring buffer will hit this).
- `module: "Preserve"` + `moduleResolution: "bundler"` + `allowImportingTsExtensions: true` → Bun-bundler mode; `.ts`/`.tsx` extension imports are fine.
- **Off** (don't re-enable): `noUnusedLocals`, `noUnusedParameters`, `noPropertyAccessFromIndexSignature`.

## Code style

- **Arrow functions only.** `const name = (...args) => ...` for top-level functions, callbacks, and React components. No `function` declarations/expressions.
- React components: `const Panel = () => { ... }`. Export both named and default if the entry (`App.tsx`) does.

## Carry-forward

- `package.json` `name` is still `bun-react-template`. Cosmetic; rename only if it bites.
- `src/index.ts` still has the demo `/api/hello` route — Phase 1 will replace it with `/api/snapshot` (+ SSE/WS in later phases per `README.md`).
